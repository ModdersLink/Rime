<UserControl x:Class="Rime.Controls.Core.NumericUpDownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rime.Controls.Core"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="360">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="13"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="13"/>
            <RowDefinition Height="13"/>
        </Grid.RowDefinitions>

        <TextBox Name="txtNumber" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" TextAlignment="Right" PreviewKeyDown="txtNumber_PreviewKeyDown" PreviewKeyUp="txtNumber_PreviewKeyUp" TextChanged="txtNumber_TextChanged"/>
        <RepeatButton Name="btnUp" Grid.Column="1" Grid.Row="0" FontSize="8" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="btnUp_Click" Content="↑"/>
        <RepeatButton Name="btnDown" Grid.Column="1" Grid.Row="1" FontSize="8" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="13" VerticalAlignment="Bottom" Click="btnDown_Click" Content="↓"/>

    </Grid>
</UserControl>
